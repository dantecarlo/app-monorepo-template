import { LoginScreen } from '@/screens/Login'

/**
 * /login route — renders the Login screen.
 * Mirrors apps/web/src/app/login/page.tsx.
 */
const LoginRoute = () => <LoginScreen />

export default LoginRoute
