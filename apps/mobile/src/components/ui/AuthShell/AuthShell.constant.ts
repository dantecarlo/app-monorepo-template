import { colors } from '@app/tokens'

export const AUTH_SHELL_GLYPH_SIZE = 28

export const AUTH_SHELL_LOGO_SIZE = 64

/** Gradient colors for the brand mark background. */
export const AUTH_SHELL_LOGO_GRADIENT: [string, string] = [
  colors.accentLight,
  colors.accent
]
