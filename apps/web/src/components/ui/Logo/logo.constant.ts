// Per-project SVG asset slot. Lives under apps/web/public (project-owned), so
// downstream projects drop in their own logo.svg without a template conflict.
export const LOGO_ASSET_SRC = '/logo.svg'

export const DEFAULT_LOGO_SIZE = 64
