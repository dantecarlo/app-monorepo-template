# Skill Registry

Auto-generated by `registry`. Do not edit manually.
Last updated: 2026-06-24

---

## Skills

### a11y

- **Description**: WCAG 2.1 AA accessibility audit for a component, screen, or scope covering four categories: semantic HTML/ARIA, keyboard navigation, visual/content, and interactive element labeling.
- **Triggers**: "a11y audit", "accessibility review", "check accessibility for [X]", "WCAG compliance", "screen reader support", "is [component] accessible?"
- **Path**: `.claude/skills/a11y/SKILL.md`

---

### arch

- **Description**: Foundational architecture reference and navigational map for the template monorepo. Encodes the fractal layer contract, file suffixes, naming conventions, import policy, Tailwind-in-constants pattern, service+adapter pairing, TanStack Query wrappers, Zustand state, and the agnostic domain layer (@app/core ports, errors, PII scrubbing).
- **Triggers**: "what's the architecture?", "what are the conventions?", "how should I name this?", "what layer does X belong to?", any coding task, "show me the folder structure", "what are the rules?"
- **Path**: `.claude/skills/arch/SKILL.md`

---

### component

- **Description**: Generate a presentational React component block with mandatory Step 0 decompose-into-wrappers gate before any file is written. Scaffolds I{Name} interface, styles constant, optional hook, arrow-const named export, and barrel.
- **Triggers**: "create component", "new component [name]", "generate component", "add a [name] component", "build the [name] UI block"
- **DO NOT USE**: for screens or data-fetching containers — use `screen` instead
- **Path**: `.claude/skills/component/SKILL.md`

---

### feature

- **Description**: Scaffold a complete coordinated feature slice across the template's real layers in dependency order: domain types → service folder → query hook → component block(s) → screen block → route registration → QueryKeys.
- **Triggers**: "scaffold feature", "create feature", "new feature [name]", "build the [name] feature end to end", "implement [name] from scratch"
- **Path**: `.claude/skills/feature/SKILL.md`

---

### fix-test

- **Description**: Migrate or repair an existing test file to current template conventions: mock-audit flow, fix wrong import source, remove data-testid, migrate inline constants, fix describe-level destructuring, remove redundant clearAllMocks, add missing axe, migrate it() to test().
- **Triggers**: "fix test", "migrate test", "update test to current patterns", "test uses wrong pattern", "repair test file", "test is broken after refactor"
- **Path**: `.claude/skills/fix-test/SKILL.md`

---

### fractal-verify

- **Description**: Audit a single built block (screen or component) against the fractal/clean architecture contract: folder structure, constants-not-inline, logic-in-hook, service+adapter pairing, no fetch in components, tests-per-unit, naming + suffix conventions, and alias imports. Read-only.
- **Triggers**: "verify this screen", "check the component architecture", "is this block fractal-compliant", "audit the fractal structure of [X]", validating a freshly scaffolded screen/component before commit
- **Path**: `.claude/skills/fractal-verify/SKILL.md`

---

### improve-skill

- **Description**: 8-dimension skill audit and tracked-change proposal with confirm-before-overwrite. Flags plugin-namespaced names as drift in the template context. Re-indexes via `registry` after edits.
- **Triggers**: "improve skill", "upgrade skill", "audit skill quality", "skill is not working well", "update [skill] instructions", "fix [skill]"
- **Path**: `.claude/skills/improve-skill/SKILL.md`

---

### lib

- **Description**: Wrap a third-party vendor behind a project-internal API (provider-behind-ports) in src/lib/{name}/ or a workspace package. Features import from @/lib/{name} or @app/*, never the vendor directly.
- **Triggers**: "wrap library", "create lib for [vendor]", "abstract [library]", "add [name] lib", "create a wrapper for [package]", "hide [vendor] behind a port"
- **Path**: `.claude/skills/lib/SKILL.md`

---

### mock

- **Description**: Create an MSW v2 handler and corresponding mock data file for an HTTP endpoint. Three-step process: typed DTO mock data → URL constant → handler registration. Reinforces PII_KEYS posture — no real sensitive data in fixtures.
- **Triggers**: "create MSW handler", "mock API endpoint", "add mock for [endpoint]", "MSW handler for [domain]", "new mock data for [resource]", "add test fixture for [name]"
- **Path**: `.claude/skills/mock/SKILL.md`

---

### modify

- **Description**: Safe edit flow for an existing component or screen: pre-flight read sequence, wrapper-detection flow, modification types (add prop, add logic, style variant, forwardRef, variant refactor at 4th boolean), and post checklist.
- **Triggers**: "modify component", "update component", "add prop to [name]", "refactor [component]", "change [component] behavior", "edit existing [name]"
- **Path**: `.claude/skills/modify/SKILL.md`

---

### new-skill

- **Description**: Authoring guide for new template skills with valid frontmatter (bare name — no plugin prefix), specific USE WHEN triggers, LLM-first step-by-step instructions, and code snippets in template conventions.
- **Triggers**: "create skill", "new skill [name]", "author skill for [task]", "write a skill that does [X]", "add a skill for [domain]"
- **Path**: `.claude/skills/new-skill/SKILL.md`

---

### perf

- **Description**: React performance review for a component or hook: unnecessary re-renders, missing/misused memoization, unstable references, async anti-patterns (prefer useAppQuery over useEffect-fetch), bundle cost, list keys, lazy init. Report-only with severity ratings.
- **Triggers**: "perf review", "performance review", "check re-renders", "optimize component", "component renders too often", "why is [X] slow", "audit performance of [name]"
- **Path**: `.claude/skills/perf/SKILL.md`

---

### pre-commit

- **Description**: Semantic staged-diff audit beyond lint/tsc: BLOCKER/WARNING/INFO verdict over service+adapter pairing, sync-prop-to-state useEffect, inline components, naming (I-prefix, arrow-only, suffixes), magic values, aria-label, && with numbers in JSX, test conventions, silent prop drop, JSX prop spreading.
- **Triggers**: "pre-commit review", "audit staged changes", "check my diff before commit", "validate changes", "review before commit", "check staged files"
- **DO NOT USE**: for post-generation full harness — use `validate-all`; for single-block structural audits use `fractal-verify`
- **Path**: `.claude/skills/pre-commit/SKILL.md`

---

### product-discovery

- **Description**: Product discovery and specification skill — clarifies scope, user stories, and acceptance criteria before implementation begins.
- **Triggers**: per the existing skill's USE WHEN phrases
- **Path**: `.claude/skills/product-discovery/SKILL.md`

---

### quality

- **Description**: Code-quality audit (report-only) mapped to docs/code-standards.md rule numbers: arrow-only, alias imports, I-prefix/{Name}Type/{Name}Enum naming, no-any, no magic values, single-object params, service+adapter pairing, barrel exports, file suffixes, no inline components, no && with numbers in JSX, aria-label, English-only, max 500 lines.
- **Triggers**: "audit code", "check quality", "validate patterns", "check conventions", "code review for [file/dir]", "quality check [scope]"
- **DO NOT USE**: for single-block structural audits — use `fractal-verify`; for post-generation harness use `validate-all`
- **Path**: `.claude/skills/quality/SKILL.md`

---

### registry

- **Description**: Skill-index generator scanning .claude/skills/ for SKILL.md files. Extracts bare skill name, USE WHEN triggers, and path. Writes .claude/skill-registry.md with confirm-before-write. Uses bat/rg/fd.
- **Triggers**: "build skill registry", "index skills", "list all skills", "what skills are available", "update skill registry", "regenerate registry"
- **Path**: `.claude/skills/registry/SKILL.md`

---

### screen

- **Description**: Generate a screen (container) block: {Name}.screen.tsx + {Name}.styles.ts + index.ts mandatory; optional components/, hooks/, models/, services/ subfolders. Uses useAppQuery/useAppMutation from @/lib/query and QueryKeys. Handles Next.js and Expo Router registration.
- **Triggers**: "create screen", "new screen [name]", "build the [name] page/screen", "scaffold the [name] route", "generate container for [name]"
- **DO NOT USE**: for presentational components — use `component` instead
- **Path**: `.claude/skills/screen/SKILL.md`

---

### security

- **Description**: Security audit repositioned to template posture: provider-behind-ports + RLS-in-adapter + scrubPII/PII_KEYS, plus edge/CDN/deployment checks (origin-lock, security headers, cache-safety, CAPTCHA, rate limiting, TLS, CDN gotchas, static-asset split). Report-only, severity-grouped. Feeds validate-all G-security group.
- **Triggers**: "audit security", "check edge security", "security review", "check origin lock", "security headers", "rate limiting", "cache safety", "check CSP", "PII exposure"
- **Path**: `.claude/skills/security/SKILL.md`

---

### service

- **Description**: Generate the full data layer for a domain: per-domain PascalCase service folder, paired service + adapter, index.ts barrel (service + adapter only), useAppQuery/useAppMutation hooks. Error handling via buildServiceError/AppError from @app/core. Arrow-only, single-object params, alias imports.
- **Triggers**: "create service", "new service [name]", "add data fetching for [name]", "hook for [name] API", "add [name] mutation", "scaffold the data layer for [name]"
- **Path**: `.claude/skills/service/SKILL.md`

---

### store

- **Description**: Generate a typed Zustand store slice in src/stores/{name}.store.ts with I{Name}State interface, named selector functions, actions, and optional persist middleware. Arrow-only, single-object action params.
- **Triggers**: "create store", "new Zustand store", "add global state for [name]", "replace context with store", "add [name] slice", "cross-feature state for [name]"
- **Path**: `.claude/skills/store/SKILL.md`

---

### test

- **Description**: Write Vitest test files for React components, hooks, helpers, and Zustand stores. Aligned to docs/code-standards.md Rule 9 and apps/*/src/test helper. Enforces project test helper imports, semantic queries, test() not it(), axe as 2nd test, MSW-first, one file per unit.
- **Triggers**: "write tests", "add tests", "create test file", "test this component/hook", "improve coverage", "test [name]"
- **DO NOT USE**: for auditing existing tests — use `fix-test`
- **Path**: `.claude/skills/test/SKILL.md`

---

### runtime-verify

- **Description**: Runtime and render smoke harness for all environments — web (Next.js), mobile-web (RNW/Expo web), mobile native bundle, iOS simulator, and Android device/emulator. Detect-and-run across five tiers with a diagnose→fix→re-verify loop on failure. Run after validate-all; covers only the runtime smokes pnpm validate excludes.
- **Triggers**: "run all smokes", "does this render?", "is the build healthy?", "runtime verify", "smoke test all environments", "before release", "verify native bundle", "check iOS simulator", "check Android device", "run runtime-verify"
- **Path**: `.claude/skills/runtime-verify/SKILL.md`

---

### validate-all

- **Description**: Post-generation deterministic harness: runs pnpm validate + five adversarial subagent groups in parallel (G-standards, G-tests, G-security, G-a11y-design-dod, G-fractal). Overall DONE only when pnpm validate passes and zero BLOCKERs across all groups.
- **Triggers**: per the existing skill's USE WHEN phrases
- **Path**: `.claude/skills/validate-all/SKILL.md`

---

24 skills indexed.
